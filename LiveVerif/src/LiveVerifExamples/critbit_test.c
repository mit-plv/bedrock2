#include <stdio.h>
#include "onesize_malloc_globals.h"
#include "onesize_malloc_exported.h"
#include "critbit_exported.h"
#include "testing.h"

int main() {
  // TODO (for now, this file just ensures that the GCC parser accepts critbit_exported.h)
}
