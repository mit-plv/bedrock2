*~
*.vo
*.glob
*.aux
.depend
.lia.cache
.nia.cache
.coqdeps.d
Makefile.coq.*
\#*\#

# _CoqProject is generated by the Makefile
_CoqProject

# I like to put
# ((nil . ((compile-command . "if git rev-parse --is-inside-work-tree ; then cgexec -g memory:myMemGroup make all -C `git rev-parse --show-superproject-working-tree --show-toplevel | head -1` -j -k ; else echo 'not sure what to do' ; fi"))))
# in my .dir-locals.el, but I don't want to force this setting on other users
.dir-locals.el
